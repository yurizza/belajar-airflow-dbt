name: 'multidimensional_modelling'
version: '1.0'
config-version: 2

profile: 'multidimensional_modelling'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  multidimensional_modelling:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
